#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to mulltiply
 * @y: the second number to be multiiply with first number
 * Return:the result of multiplication between x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
